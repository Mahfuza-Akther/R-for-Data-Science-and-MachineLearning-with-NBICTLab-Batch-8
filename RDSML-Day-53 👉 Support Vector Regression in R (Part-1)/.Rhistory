setwd("~/Desktop/GitHub practice/R-for-Data-Science-and-MachineLearning-with-NBICTLab-Batch-8/RDSML-Day-53 👉 Support Vector Regression in R (Part-1)")
# Setup : pacakages and data loading
installed.packages(c("e1071", "ggplot2", "dplyr"))
install.packages(c("e1071", "ggplot2", "dplyr"))
library(e1071)
library(ggplot2)
library(dplyr)
dataset <- read.csv("day.csv")
View(dataset)
bike <- dataset %>%
select(cnt, temp, aTemp, hum, windspeed, season, yr, mnth, holiday,
workingday, weathersit)
# Basic cleaning and feature selection
bike <- dataset %>%
select(cnt, temp, atemp, hum, windspeed, season, yr, mnth, holiday,
workingday, weathersit)
bike <- bike %>%
mutate(
season = factor(season),
yr = Factor(yr),
mnth = factor(mnth),
holiday = factor(holiday),
workingday = factor(workingday),
weathersit = factor(weathersit)
)
bike <- bike %>%
mutate(
season = factor(season),
yr = factor(yr),
mnth = factor(mnth),
holiday = factor(holiday),
workingday = factor(workingday),
weathersit = factor(weathersit)
)
View(bike)
str(bike)
View(dataset)
bike <- dataset %>%
select(cnt, temp, atemp, hum, windspeed, season, yr, mnth, holiday,
weekday, workingday, weathersit)
library(e1071)
library(ggplot2)
library(dplyr)
dataset <- read.csv("day.csv")
bike <- dataset %>%
select(cnt, temp, atemp, hum, windspeed, season, yr, mnth, holiday,
weekday, workingday, weathersit)
bike <- bike %>%
mutate(
season = factor(season),
yr = factor(yr),
mnth = factor(mnth),
holiday = factor(holiday),
weekday = factor(weekday),
workingday = factor(workingday),
weathersit = factor(weathersit)
)
str(bike)
n <- nrow(bike)
n
seq_len(n)
indices <- seq_len(n)
indices
train_idx <- sample(indices, size = 0.7 * n)
train_idx
train_set <- bike[train_idx, ]
View(train_set)
test_set <- bike[-train_idx, ]
View(test_set)
View(test_set)
